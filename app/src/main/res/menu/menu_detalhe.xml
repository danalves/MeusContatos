<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/iDeletar"
        android:title="@string/del_menu2"
        android:icon="@drawable/ic_deletar"
        app:showAsAction="always"/>

    <item android:id="@+id/iEditar"
        android:title="@string/edit_menu2"
        android:icon="@drawable/ic_editar"
        app:showAsAction="always"/>

    <item android:id="@+id/iCompartilhar"
        android:title="@string/share_menu"
        android:icon="@drawable/ic_share"
        app:showAsAction="ifRoom"/>

</menu>